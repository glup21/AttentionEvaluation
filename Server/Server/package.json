{
  "name": "stressManagementServer",
  "version": "1.0.0",
  "description": "Basic Express.js server to accept data from Unreal Engine and, possibly, another sources, processes them and sends to React app.",
  "license": "GPL-3.0-only",
  "author": "gl_up21",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.1.0",
    "stressManagementServer": "file:"
  }
}
